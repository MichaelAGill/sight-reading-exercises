import { type Pitch, type Harmony, natural, midi, mod } from './model';
const keys:Record<string,[string,number,number]>={C:['C',0,0],G:['G',0,1],F:['F',0,-1],D:['D',0,2],A:['A',0,3],E:['E',0,4],B:['B',0,5],'B-flat':['B',-1,-2],'E-flat':['E',-1,-3],'A-flat':['A',-1,-4],'D-flat':['D',-1,-5],'F-sharp':['F',1,6],'C-sharp':['C',1,7]};
export function keyInfo(key:string) {const [name,mode]=key.split(' ');const [step,alter,majorFifths]=keys[name];return {tonic:{step:step as Pitch['step'],alter,octave:4},fifths:majorFifths-(mode==='minor'?3:0),mode:mode as 'major'|'minor'};}
export function degreePitch(tonic:Pitch,mode:'major'|'minor',degree:number,octave=4,raised=false):Pitch {const letters='CDEFGAB';const index=letters.indexOf(tonic.step)+degree;const step=letters[mod(index,7)] as Pitch['step'];const oct=octave+Math.floor(index/7);const scale=mode==='major'?[0,2,4,5,7,9,11]:[0,2,3,5,7,8,10];const value=12*(octave+1)+natural[tonic.step]+tonic.alter+12*Math.floor(degree/7)+scale[mod(degree,7)]+(raised?1:0);return {step,octave:oct,alter:value-(12*(oct+1)+natural[step])};}
export const chordDegrees=(h:Harmony):number[]=>h==='I'?[0,2,4]:h==='ii'?[1,3,5]:h==='IV'?[3,5,0]:h==='V'?[4,6,1]:h==='vi'?[5,0,2]:[1,3,5];
export function isChordTone(p:Pitch,tonic:Pitch,mode:'major'|'minor',h:Harmony) {return chordDegrees(h).some(d=>mod(midi(degreePitch(tonic,mode,d,4,(h==='V'&&mode==='minor'&&d===6)||(h==='V/V'&&d===3))),12)===mod(midi(p),12));}
export const keyAlter=(fifths:number,step:Pitch['step'])=>fifths>0?('FCGDAEB'.slice(0,fifths).includes(step)?1:0):('BEADGCF'.slice(0,-fifths).includes(step)?-1:0);
export function seeded(seed:number) {let a=seed>>>0;return ()=>{a+=0x6D2B79F5;let t=a;t=Math.imul(t^(t>>>15),t|1);t^=t+Math.imul(t^(t>>>7),t|61);return ((t^(t>>>14))>>>0)/4294967296;};}
